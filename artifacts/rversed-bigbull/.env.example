# Optional: set this to the API origin when the API is deployed separately.
# Example: https://api.example.com
# Do not append /api; generated requests already include that path.
# Leave unset to use same-origin /api/... requests behind a reverse proxy.
VITE_API_BASE_URL=